const Dev = require('../models/Dev');

module.exports = {

    /**
     * Controller for Search Devs in range location
     * @param {*} request 
     * @param {*} response 
     */
    async index(request, response) {        
        // Buscar todos os devs num raio de 10km
        // Filtrar também por tecnologia.
        
    },

};